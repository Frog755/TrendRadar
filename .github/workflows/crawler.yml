name: Hot News Crawler

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write     # 必须有这个权限才能创建 PR

jobs:
  crawl:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Verify required files
        run: |
          echo "🔍 检查必需的配置文件..."
          [ -f config/config.yaml ] || (echo "❌ config/config.yaml 不存在" && exit 1)
          [ -f config/frequency_words.txt ] || (echo "❌ config/frequency_words.txt 不存在" && exit 1)
          echo "✅ 配置文件检查通过"

      - name: Run crawler
        env:
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DINGTALK_WEBHOOK_URL: ${{ secrets.DINGTALK_WEBHOOK_URL }}
          WEWORK_WEBHOOK_URL: ${{ secrets.WEWORK_WEBHOOK_URL }}
          WEWORK_MSG_TYPE: ${{ secrets.WEWORK_MSG_TYPE }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          EMAIL_SMTP_SERVER: ${{ secrets.EMAIL_SMTP_SERVER }}
          EMAIL_SMTP_PORT: ${{ secrets.EMAIL_SMTP_PORT }}
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
          NTFY_SERVER_URL: ${{ secrets.NTFY_SERVER_URL }}
          NTFY_TOKEN: ${{ secrets.NTFY_TOKEN }}
          BARK_URL: ${{ secrets.BARK_URL }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          GITHUB_ACTIONS: true
        run: python main.py

      - name: Create new branch & commit changes
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

          BRANCH="crawler-update-$(date +'%Y%m%d-%H%M%S')-${{ github.run_id }}"
          git checkout -b "$BRANCH"
          git add -A
          
          if git diff --staged --quiet; then
            echo "✅ 没有文件改动，跳过提交"
            echo "NO_CHANGES=true" >> $GITHUB_ENV
          else
            git commit -m "🤖 Auto crawled data update $(date '+%Y-%m-%d %H:%M:%S %Z')"
            git push origin "$BRANCH"
            echo "NO_CHANGES=false" >> $GITHUB_ENV
          fi

      - name: Create Pull Request
        if: env.NO_CHANGES != 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: crawler-update-${{ github.run_id }}-${{ github.run_attempt }}
          base: master
          title: "🤖 热点新闻自动更新 $(date '+%Y-%m-%d %H:%M:%S')"
          body: |
            自动化爬虫任务已完成
            
            Workflow #${{ github.run_number }} · Run ${{ github.run_id }}
          commit-message: "🤖 Auto crawled data update"
          delete-branch: true
